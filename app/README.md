# app/

Streamlit dashboard (fast path). One file to start: `streamlit_app.py`. Reads `../data/artifacts/*.parquet` and `eval.json`. Runs with `streamlit run app/streamlit_app.py`. Must work with the network off.
