# backend/

Thin FastAPI wrapper over `../data/artifacts/*.parquet` so the deliverable is both a dashboard and an API. Target ~40 lines: `GET /confidence?variable=&lead=&init=`, `GET /eval`, `GET /case-studies`. Added last, after the Streamlit app works.
