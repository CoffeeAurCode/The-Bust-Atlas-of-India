# frontend/

Reserved for a React + MapLibre front end. **Empty on the fast path** — the dashboard is `app/streamlit_app.py`. Only build this if the Streamlit MVP is done, submitted, and someone wants polish.
